"""CLI entry-point scaffold for future run, transcribe, analyze, and render commands."""


def main() -> None:
    """Expose an install-verification command without implementing pipeline behavior."""
    print("autocut Stage 0 scaffold; no pipeline commands are implemented yet.")
